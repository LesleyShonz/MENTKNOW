import { SelectTool } from "./SelectTool/SelectTool.mjs";
import { StateNode } from "./StateNode.mjs";
import { ZoomTool } from "./ZoomTool/ZoomTool.mjs";
class RootState extends StateNode {
  static id = "root";
  static initial = "select";
  static children = () => [SelectTool, ZoomTool];
  onKeyDown = (info) => {
    switch (info.code) {
      case "KeyZ": {
        if (!(info.shiftKey || info.ctrlKey)) {
          const currentTool = this.current.value;
          if (currentTool && currentTool.current.value?.id === "idle") {
            this.editor.setSelectedTool("zoom", { ...info, onInteractionEnd: currentTool.id });
          }
        }
        break;
      }
    }
  };
}
export {
  RootState
};
//# sourceMappingURL=RootState.mjs.map
