"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var Shape_exports = {};
__export(Shape_exports, {
  Shape: () => Shape
});
module.exports = __toCommonJS(Shape_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_primitives = require("@tldraw/primitives");
var React = __toESM(require("react"));
var import_signia_react = require("signia-react");
var import__ = require("../..");
var import_useEditorComponents = require("../hooks/useEditorComponents");
var import_useQuickReactor = require("../hooks/useQuickReactor");
var import_useShapeEvents = require("../hooks/useShapeEvents");
var import_ErrorBoundary = require("./ErrorBoundary");
const Shape = (0, import_signia_react.track)(function Shape2({
  id,
  index,
  backgroundIndex,
  opacity,
  isCulled
}) {
  const editor = (0, import__.useEditor)();
  const { ShapeErrorFallback } = (0, import_useEditorComponents.useEditorComponents)();
  const events = (0, import_useShapeEvents.useShapeEvents)(id);
  const containerRef = React.useRef(null);
  const backgroundContainerRef = React.useRef(null);
  const setProperty = React.useCallback((property, value) => {
    containerRef.current?.style.setProperty(property, value);
    backgroundContainerRef.current?.style.setProperty(property, value);
  }, []);
  (0, import_useQuickReactor.useQuickReactor)(
    "set shape container transform position",
    () => {
      const shape2 = editor.getShapeById(id);
      const pageTransform = editor.getPageTransformById(id);
      if (!shape2 || !pageTransform)
        return null;
      const transform = import_primitives.Matrix2d.toCssString(pageTransform);
      setProperty("transform", transform);
    },
    [editor, setProperty]
  );
  (0, import_useQuickReactor.useQuickReactor)(
    "set shape container clip path",
    () => {
      const shape2 = editor.getShapeById(id);
      if (!shape2)
        return null;
      const clipPath = editor.getClipPathById(id);
      setProperty("clip-path", clipPath ?? "none");
    },
    [editor, setProperty]
  );
  (0, import_useQuickReactor.useQuickReactor)(
    "set shape height and width",
    () => {
      const shape2 = editor.getShapeById(id);
      if (!shape2)
        return null;
      const util2 = editor.getShapeUtil(shape2);
      const bounds = util2.bounds(shape2);
      setProperty("width", Math.ceil(bounds.width) + "px");
      setProperty("height", Math.ceil(bounds.height) + "px");
    },
    [editor]
  );
  React.useLayoutEffect(() => {
    setProperty("opacity", opacity + "");
    containerRef.current?.style.setProperty("z-index", index + "");
    backgroundContainerRef.current?.style.setProperty("z-index", backgroundIndex + "");
  }, [opacity, index, backgroundIndex, setProperty]);
  const shape = editor.getShapeById(id);
  if (!shape)
    return null;
  const util = editor.getShapeUtil(shape);
  return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    util.renderBackground && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
      "div",
      {
        ref: backgroundContainerRef,
        className: "tl-shape tl-shape-background",
        "data-shape-type": shape.type,
        draggable: false,
        children: !isCulled && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
          import_ErrorBoundary.OptionalErrorBoundary,
          {
            fallback: ShapeErrorFallback ? (error) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ShapeErrorFallback, { error }) : null,
            onError: (error) => editor.annotateError(error, { origin: "react.shape", willCrashApp: false }),
            children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InnerShapeBackground, { shape, util })
          }
        )
      }
    ),
    /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
      "div",
      {
        ref: containerRef,
        className: "tl-shape",
        "data-shape-type": shape.type,
        draggable: false,
        onPointerDown: events.onPointerDown,
        onPointerMove: events.onPointerMove,
        onPointerUp: events.onPointerUp,
        onPointerEnter: events.onPointerEnter,
        onPointerLeave: events.onPointerLeave,
        children: isCulled && util.canUnmount(shape) ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CulledShape, { shape, util }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
          import_ErrorBoundary.OptionalErrorBoundary,
          {
            fallback: ShapeErrorFallback ? (error) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ShapeErrorFallback, { error }) : null,
            onError: (error) => editor.annotateError(error, { origin: "react.shape", willCrashApp: false }),
            children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InnerShape, { shape, util })
          }
        )
      }
    )
  ] });
});
const InnerShape = React.memo(
  function InnerShape2({ shape, util }) {
    return (0, import_signia_react.useStateTracking)("InnerShape:" + util.type, () => util.component(shape));
  },
  (prev, next) => prev.shape.props === next.shape.props
);
const InnerShapeBackground = React.memo(
  function InnerShapeBackground2({
    shape,
    util
  }) {
    return (0, import_signia_react.useStateTracking)("InnerShape:" + util.type, () => util.renderBackground?.(shape));
  },
  (prev, next) => prev.shape.props === next.shape.props
);
const CulledShape = React.memo(
  function CulledShap({ shape, util }) {
    const bounds = util.bounds(shape);
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
      "div",
      {
        className: "tl-shape__culled",
        style: {
          transform: `translate(${bounds.minX}px, ${bounds.minY}px)`,
          width: bounds.width,
          height: bounds.height
        }
      }
    );
  },
  () => true
);
//# sourceMappingURL=Shape.js.map
