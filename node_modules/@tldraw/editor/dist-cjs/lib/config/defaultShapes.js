"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var defaultShapes_exports = {};
__export(defaultShapes_exports, {
  checkShapesAndAddCore: () => checkShapesAndAddCore,
  coreShapes: () => coreShapes,
  defaultShapes: () => defaultShapes
});
module.exports = __toCommonJS(defaultShapes_exports);
var import_ArrowShape = require("../editor/shapes/arrow/ArrowShape");
var import_BookmarkShape = require("../editor/shapes/bookmark/BookmarkShape");
var import_DrawShape = require("../editor/shapes/draw/DrawShape");
var import_EmbedShape = require("../editor/shapes/embed/EmbedShape");
var import_FrameShape = require("../editor/shapes/frame/FrameShape");
var import_GeoShape = require("../editor/shapes/geo/GeoShape");
var import_GroupShape = require("../editor/shapes/group/GroupShape");
var import_HighlightShape = require("../editor/shapes/highlight/HighlightShape");
var import_ImageShape = require("../editor/shapes/image/ImageShape");
var import_LineShape = require("../editor/shapes/line/LineShape");
var import_NoteShape = require("../editor/shapes/note/NoteShape");
var import_TextShape = require("../editor/shapes/text/TextShape");
var import_VideoShape = require("../editor/shapes/video/VideoShape");
const coreShapes = [
  // created by grouping interactions, probably the corest core shape that we have
  import_GroupShape.GroupShape,
  // created by embed menu / url drop
  import_EmbedShape.EmbedShape,
  // created by copy and paste / url drop
  import_BookmarkShape.BookmarkShape,
  // created by copy and paste / file drop
  import_ImageShape.ImageShape,
  // created by copy and paste
  import_TextShape.TextShape
];
const defaultShapes = [
  import_DrawShape.DrawShape,
  import_GeoShape.GeoShape,
  import_LineShape.LineShape,
  import_NoteShape.NoteShape,
  import_FrameShape.FrameShape,
  import_ArrowShape.ArrowShape,
  import_HighlightShape.HighlightShape,
  import_VideoShape.VideoShape
];
const coreShapeTypes = new Set(coreShapes.map((s) => s.type));
function checkShapesAndAddCore(customShapes) {
  const shapes = [...coreShapes];
  const addedCustomShapeTypes = /* @__PURE__ */ new Set();
  for (const customShape of customShapes) {
    if (coreShapeTypes.has(customShape.type)) {
      throw new Error(
        `Shape type "${customShape.type}" is a core shapes type and cannot be overridden`
      );
    }
    if (addedCustomShapeTypes.has(customShape.type)) {
      throw new Error(`Shape type "${customShape.type}" is defined more than once`);
    }
    shapes.push(customShape);
    addedCustomShapeTypes.add(customShape.type);
  }
  return shapes;
}
//# sourceMappingURL=defaultShapes.js.map
